// src/config.js

// const BASE_URL = "http://localhost:8000/api";  // Change this URL if your backend is deployed elsewhere
const BASE_URL = "https://tickting-system-backend.onrender.com/api"; // Use the deployed URL for the backend
export default BASE_URL;
